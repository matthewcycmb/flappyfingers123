// ── Shared Leaderboard (disabled) ───────────────────────
// To enable later, add Supabase/Firebase integration here.
// The game uses the local leaderboard in game/score.js as fallback.

export async function initLeaderboard() {}
export async function submitScore() {}
export async function getTopScores() { return null; }
export function isOnline() { return false; }
